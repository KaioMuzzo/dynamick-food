# Base URL of the Express backend the app talks to.
# Only EXPO_PUBLIC_-prefixed vars are exposed to the app, and they ship inside the
# compiled binary — never put a secret or a server-only API key here.
EXPO_PUBLIC_API_URL=http://localhost:3333/api
